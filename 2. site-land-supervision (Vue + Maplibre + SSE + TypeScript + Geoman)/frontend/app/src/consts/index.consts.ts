import { GeometryStylesConsts } from './geometryStyles.consts'
import { HotKeys } from './hotKey.consts'
import { LayerTypes, mapStyles, rowName } from './map.consts'
import { ErrorMessages } from './message.consts'
import { RouterNames, ROUTES } from './router.consts'
import { activeCursors, activeTools, messageForUser } from './toolbar.consts'
import { API_PREFIX, DictsPrefixAPI } from '@/consts/api.consts.ts'

export const Consts: any = {
  HotKeys,
  GeometryStylesConsts,
  ROUTES,
  RouterNames,
  activeTools,
  activeCursors,
  messageForUser,
  mapStyles,
  ErrorMessages,
  LayerTypes,
  API_PREFIX,
  DictsPrefixAPI,
  rowName
}
