// const texts = ["Webentwicklung", "Mobile Apps", "ki", "Webshops", "white label"];
// let index = 0;
// const span = document.getElementById("scrollingText");
//
// function changeText() {
//     // Добавляем класс для анимации
//     span.classList.add("scrolling");
//
//     // Убираем класс через время анимации, чтобы была возможность повторного использования
//     setTimeout(() => {
//         span.classList.remove("scrolling");
//         index = (index + 1) % texts.length; // Обновляем индекс текста
//         span.textContent = texts[index];    // Меняем текст
//     }, 1000); // Время совпадает с длительностью анимации (1с)
// }
//
// // Меняем текст каждую секунду
// setInterval(changeText, 2000);
